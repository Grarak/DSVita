float4 out gl_Position : POSITION;
float2 out texCoords : TEXCOORD0;
float out texFactor : TEXCOORD1;

void main(float2 position, float3 texCoordsColor) {
    texCoords = texCoordsColor.xy;
    texFactor = texCoordsColor.z;
    gl_Position = float4(position, 0.0, 1.0);
}
