float2 in texCoords : TEXCOORD0;

uniform sampler2D tex : TEXUNIT0;

uniform float alpha;

void main(out float4 color : COLOR) {
    float a = tex2D(tex, texCoords).r;
    if (a <= 0.0) {
        discard;
    }
    color = float4(1.0, 1.0, 1.0, a * alpha);
}
