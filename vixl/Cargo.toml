[package]
name = "vixl"
version = "0.1.0"
edition = "2024"

[dependencies]

[build-dependencies]
regex = "1.11.1"
bindgen = "0.72.1"
regex = "1.11.1"

vitabuild = { path = "../vitabuild" }

[target.'cfg(target_arch = "aarch64")'.dev-dependencies]
libc = "0.2"
